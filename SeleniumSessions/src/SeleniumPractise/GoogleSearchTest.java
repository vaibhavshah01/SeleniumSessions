package SeleniumPractise;

public class GoogleSearchTest {

	public static void main(String[] args) {
		
		// how do perform google search? // scenario based question
		// ex. in google search type : testing ( few suggestions comes up) one suggestion testing tools
		// index not define for testing tools suggestion , pick specific value from suggestion list and iterate the list
		// we use driver.findElement ( // xpath for testing).sendkeys("testing)
		// now we have mamy suggestions 
		// we use driver.findElements ( // xpath for ul all suggestion ) and we store in List<WebElement>
		// we print size of list : list.size
		// for iterate the list we use for loop
		// we use descendant and we put in li look for specific div
		

	}

}
